package com.example.vigoshorts.search;

public class SearchAPIManager {
    private static final String SEARCH_API_URL = "https://vivekwebsiteapi.000webhostapp.com/search.php";

/*    public static void searchUser(String query, ResponseListener<List<User>> listener) {
        // Make API request to search for users based on the query
        // Parse the response and convert it into a list of User objects
        // Invoke the listener's onSuccess method with the list of users
        // In case of an error, invoke the listener's onError method with the error message

    }

    public static void searchHashtag(String query, ResponseListener<List<Hashtag>> listener) {
        // Make API request to search for hashtags based on the query
        // Parse the response and convert it into a list of Hashtag objects
        // Invoke the listener's onSuccess method with the list of hashtags
        // In case of an error, invoke the listener's onError method with the error message
    }

    public static void searchMusic(String query, ResponseListener<List<Music>> listener) {
        // Make API request to search for music based on the query
        // Parse the response and convert it into a list of Music objects
        // Invoke the listener's onSuccess method with the list of music
        // In case of an error, invoke the listener's onError method with the error message
    }

    public static void searchVideos(String query, ResponseListener<List<Video>> listener) {
        // Make API request to search for videos based on the query
        // Parse the response and convert it into a list of Video objects
        // Invoke the listener's onSuccess method with the list of videos
        // In case of an error, invoke the listener's onError method with the error message
    }*/
}

